---
import IconLocation from "../../public/assets/icon/IconLocation.astro";
import { proyects } from "../data/proyectsData";

const allProyects = proyects;
---

<div class="stack-cards-wrapper flex flex-col gap-4">
    {
        allProyects.map(
            (
                {
                    titleProyect,
                    subtitle,
                    description,
                    location,
                    status,
                    bgImage,
                },
                index,
            ) => (
                <div class="stack-card-slot" data-card-slot={index}>
                    <section
                        class={`stack-card relative w-full md:h-150 h-125 rounded-4xl flex justify-end md:items-center items-end ${bgImage} bg-cover md:bg-center bg-bottom md:px-4`}
                        data-stack-index={index}
                    >
                        {/* Overlay solo sobre el fondo */}
                        <div class="absolute inset-0 rounded-4xl bg-black/25 pointer-events-none" />

                        {/* Card por encima del overlay */}
                        <div class="card-content relative z-10 bg-white md:px-12 p-6 md:w-135 w-full md:h-95 md:max-h-none max-h-fit mr-0 md:mr-10 xl:mr-22 rounded-4xl flex flex-col justify-center shadow-2xl">
                            <header class="md:mb-6 mb-2">
                                <h1 class="lg:text-4xl text-2xl font-bold md:mb-5 mb-2 text-primary-blue">
                                    {titleProyect}
                                </h1>
                                <p class="mb-2 md:text-xl text-base font-bold">{subtitle}</p>
                                <p class="mb-2 md:text-xl text-base font-semibold text-[#5D5D5D] text-pretty">
                                    {description}
                                </p>
                            </header>
                            <footer class="flex md:flex-row flex-col justify-between gap-6 md:items-center h-max">
                                <span class="font-semibold md:text-lg text-base flex w-full items-center gap-1">
                                    <IconLocation /> {location}
                                </span>
                                <span class="font-extrabold text-[12px] bg-primary-blue text-white py-2 px-6 rounded-full w-max">
                                    {status}
                                </span>
                            </footer>
                        </div>
                    </section>
                </div>
            ),
        )
    }
</div>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const slots = Array.from(
            document.querySelectorAll("[data-card-slot]"),
        ) as HTMLElement[];

        slots.forEach((slot, index) => {
            const card = slot.querySelector(".stack-card") as HTMLElement;
            if (!card) return;

            // Configuración sticky
            card.style.position = "sticky";
            card.style.top = `${100 + index * 40}px`;
            card.style.zIndex = (slots.length - index).toString();
            card.style.transition = "all 0.6s cubic-bezier(0.4, 0, 0.2, 1)";

            // Estado inicial
            const initialScale = 1 - index * 0.05;
            card.style.transform = `scale(${initialScale})`;
            card.style.opacity = "0.6";

            // Observer
            const observer = new IntersectionObserver(
                (entries) => {
                    entries.forEach((entry) => {
                        if (entry.isIntersecting) {
                            card.style.transform = "scale(1)";
                            card.style.opacity = "1";
                        } else {
                            const initialScale = 1 - index * 0.05;
                            card.style.transform = `scale(${initialScale})`;
                            card.style.opacity = "0.6";
                        }
                    });
                },
                {
                    threshold: 0.5,
                    rootMargin: "-100px 0px",
                },
            );

            observer.observe(slot);
        });
    });
</script>

<style>
    .stack-cards-wrapper {
        position: relative;
        width: 100%;
        height: max-content;
    }

    .stack-card-slot {
        height: 100%;
        position: relative;
    }

    .card-content {
        transform-origin: center;
        transition: transform 0.3s ease;
    }

    .stack-card:hover .card-content {
        transform: translateY(-10px);
    }
</style>
